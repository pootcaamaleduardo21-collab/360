# ── Supabase ─────────────────────────────────────────────────────────────────
# Get these from: https://app.supabase.com → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...

# Service-role key — NEVER expose this to the browser (no NEXT_PUBLIC_ prefix).
# Used ONLY in server-side API routes (e.g. /api/admin/stats).
# Get it from: Supabase Dashboard → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...

# ── App ───────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Super Admin ────────────────────────────────────────────────────────────────
# The email that always receives super_admin role regardless of DB metadata.
# Set this to YOUR email address.
NEXT_PUBLIC_SUPER_ADMIN_EMAIL=tu@email.com

# ── Optional: Cloudinary (alternative CDN) ────────────────────────────────────
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

# ── Optional: Google Maps Embed API (POI route maps) ─────────────────────────
# Enables inline route maps in the POI panel — FREE with Maps Embed API.
# Get your key at: https://console.cloud.google.com → APIs → Maps Embed API
# NEXT_PUBLIC_GOOGLE_MAPS_KEY=AIza...


#lo que puede hacer como para que no le muestre los archivos a cada usuario seria hacer como un bot donde gestione los correos
#ejemplo: registro - bot - bot lo renombra como usuario independiente - selecciona carpeta independiente - carpeta sin visualizacion
#el administrador de los formatos o archivos puede verlo, no los usuarios, como una piramide.
#ejemplo 2: piramide:}
#base: son los usuarios. Siguiente fila de la piramide: registro, penultima fila: bot de gestion y separador de docx, punta:
#admin, no se algo asi, aun que como no esta desarrollado como tal algun gestion de manejo de datos lo mas probable sea de que
# se organice de manera manual todos estos documentos de usuarios y administradores de la app/pagina
#seria buena idea desarrollar el gestor de archivos sencillo pero tambien seria algo complicado hacer el bot donde separe y 
#realice todo esto por que teniendo en cuenta que pueden ser miles de datos podria ser metadatos donde por medio de excel 
# O algun otro tipo de donde guardar muchos archivos sea mas adaptable, mas sencillo y con flexibilidad en desarrollo


#en teoria: el bot, solo seria metodo de organizacion de metadatos en este proyecto
#se llama: cifradoi de conocimiento cero: algo asi :v